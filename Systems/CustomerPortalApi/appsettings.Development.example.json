{
  "CustomerPortal": {
    "ConnectionString": "Server=51.17.121.203;Database=Calibrator;User Id=app_stage;Password=REPLACE_ME;Encrypt=True;TrustServerCertificate=True",

    "//": "Generate each secret with: openssl rand -base64 32   (min. 32 characters)",
    "OtpPepper": "REPLACE_ME_32_PLUS_CHARACTERS",

    "//SessionSecret": "Must be byte-identical to CUSTOMER_SESSION_SECRET in the web app, which verifies the same cookie.",
    "SessionSecret": "REPLACE_ME_32_PLUS_CHARACTERS",

    "AllowedOrigins": [ "http://localhost:3000" ],
    "//SecureCookies": "false only because local development is plain HTTP.",
    "SecureCookies": false,

    "Smtp": {
      "Host": "smtp.office365.com",
      "Port": 587,
      "UseImplicitTls": false,
      "User": "zimun@mba.co.il",
      "Password": "REPLACE_ME",
      "From": "zimun@mba.co.il"
    }
  }
}
